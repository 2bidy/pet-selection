import 'package:flutter/material.dart';
import 'package:pet_selection/ui/views/user_access/user_access_components.dart';

////////////////////////////////////////////////////////////////////////
// HANDLES DESKTOP SCREEN
////////////////////////////////////////////////////////////////////////

class UserAccessPage4Desktop extends StatelessWidget {
  const UserAccessPage4Desktop({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AuthContainer();
  }
}

////////////////////////////////////////////////////////////////////////
// HANDLES TABLET SCREEN PORTRAIT
////////////////////////////////////////////////////////////////////////

class UserAccessPage4TabletPortrait extends StatelessWidget {
  const UserAccessPage4TabletPortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AuthContainer();
  }
}

////////////////////////////////////////////////////////////////////////
// HANDLES MOBILE SCREEN PORTRAIT
////////////////////////////////////////////////////////////////////////

class UserAccessPage4MobilePortrait extends StatelessWidget {
  const UserAccessPage4MobilePortrait({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return AuthContainer();
  }
}

////////////////////////////////////////////////////////////////////////
// HANDLES MOBILE SCREEN LANDSCAPE
////////////////////////////////////////////////////////////////////////

class UserAccessPage4MobileLandscape extends StatelessWidget {
  const UserAccessPage4MobileLandscape({Key? key}) : super(key: key);

  @override /*color: white,
      height: 500.0,*/
  Widget build(BuildContext context) {
    return AuthContainer();
  }
}
